import styles from './Application.module.css';

const Application = () => {
  return (
    <main className={styles.Wrapper}>
      <h1>Hello World</h1>
    </main>
  );
};

export default Application;
